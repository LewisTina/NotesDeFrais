<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NotesDeFrais.Pages.SessionPage"
             BackgroundColor="{StaticResource Surface}"
             Title="Session">
    <ScrollView>
        <VerticalStackLayout Padding="28,24" Spacing="18">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16">
                <VerticalStackLayout Spacing="3">
                    <Label Text="Espace NotesDeFrais"
                           Style="{StaticResource PageTitle}" />
                    <Label Text="Connexion a ton espace notes de frais"
                           Style="{StaticResource PageSubtitle}" />
                </VerticalStackLayout>
                <ActivityIndicator Grid.Column="1"
                                   IsRunning="{Binding IsBusy}"
                                   IsVisible="{Binding IsBusy}"
                                   VerticalOptions="Center" />
            </Grid>

            <Border Style="{StaticResource Panel}">
                <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="14">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{Binding SessionTitle}"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Ink}" />
                        <Label Text="{Binding Email}"
                               TextColor="{StaticResource Muted}" />
                    </VerticalStackLayout>
                    <Border Grid.Column="1"
                            Padding="12,7"
                            BackgroundColor="{StaticResource AccentSoft}"
                            StrokeThickness="0"
                            VerticalOptions="Start">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <Label Text="{Binding RoleLabel}"
                               TextColor="{StaticResource Accent}"
                               FontAttributes="Bold"
                               FontSize="12" />
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource Panel}">
                <VerticalStackLayout Spacing="14">
                    <Label Text="Acces au compte"
                           Style="{StaticResource SectionTitle}" />
                    <Label Text="{Binding ConfigurationMessage}"
                           TextColor="{StaticResource Muted}" />
                    <Border BackgroundColor="{StaticResource DangerSoft}"
                            IsVisible="{Binding IsConfigurationMissing}"
                            Padding="12"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <Label Text="La connexion est desactivee tant que la configuration de connexion n'est pas disponible."
                               TextColor="{StaticResource Danger}" />
                    </Border>

                    <FlexLayout Direction="Row" Wrap="Wrap" AlignItems="Center">
                        <Button Text="Connexion Cognito"
                                Margin="0,0,10,10"
                                IsEnabled="{Binding IsConfigurationReady}"
                                IsVisible="{Binding IsConnected, Converter={StaticResource InvertedBoolConverter}}"
                                Clicked="OnSignInClicked" />
                        <Button Text="Deconnexion"
                                Style="{StaticResource DangerButton}"
                                Margin="0,0,10,10"
                                IsVisible="{Binding IsConnected}"
                                Clicked="OnSignOutClicked" />
                    </FlexLayout>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
